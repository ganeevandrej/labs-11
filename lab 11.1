#include <stdio.h>

int main()
{
	int  A, B;
	scanf_s("%d %d", &A, &B);
	if (A == B) {
		A = 0;
		B = 0;
	}
	if (A != B) {
		if (A > B)
			B = A;
		else
			A = B;
	}

	printf("%d %d", A, B);
	return 0;
}
