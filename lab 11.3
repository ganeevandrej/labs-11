#include <stdio.h>
#include <math.h>
int main()
{
	float  A, B, C, a, b;
	scanf_s("%f %f %f", &A, &B, &C);
	a = abs(A - B);
	b = abs(A - C);
	if (a < b)
	printf("%.1f %.1f", C, b);
	if (a >= b)
		printf("%.1f %.1f", B, a);
	return 0;
}
